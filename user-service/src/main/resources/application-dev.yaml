spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user-service-test
    hikari:
      username: postgres
      password: changeme

  jpa:
    hibernate:
      ddl-auto: update


logging:
  level:
    root: info
    sql: debug

server:
  port: 8082
