
replicaCount: 1

service:
  ports:
    client: 9092

persistence:
  enabled: true
  storageClass: local-path
  size: 250Mi

serviceAccount:
  create: true
rbac:
  create: true

# provisioning: 
#   enabled: true
#   topics:
#     - ""

kraft:
  enabled: false

zookeeper:
  enabled: true
  replicaCount: 1
  persistence:
    enabled: true
    storageClass: local-path
    size: 200Mi
