spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/group-service-test
    hikari:
          username: postgres
          password: changeme

#    url: jdbc:h2:mem:group-service-test
#    username: spring
#    password: pass

  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: info
    sql: debug

jwt.secret: "random-key-from-env-var"

cors:
  mappings:
    - url: "/**"
      originPatterns: "http://localhost:[*]"
      allowedMethods: "*"
      allowedHeaders: "*"
      exposedHeaders: "Authorization"
      allowCredentials: true

server:
  port: 8083



