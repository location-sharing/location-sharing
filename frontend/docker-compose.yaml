version: "3.8"
services:
  caddy:
    image: caddy:2.7
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./build:/srv
      - caddy_data:/data

volumes:
  caddy_data:
    external: true